# План улучшения проекта: что сделано и что осталось

Дата: 2026-09-01. Основа — аудит безопасности бота и аудит доступности
и производительности сайта, проведённые в этой сессии.

## Сделано

### Спринт 1 — фундамент

- **CI.** `.github/workflows/ci.yml` — три независимые job'ы: сайт (30
  тестов), бот (141 тест + typecheck), Python-инструменты (126 тестов).
  До этого всё гонялось только руками.
- **CHECK на статусы заявки.** Миграция `0002_leads_status_check.sql`
  пересобирает `leads` с ограничениями на `status` и `delivery_status`.
- **Чистка журнала.** `lead_events` рос без ограничения — теперь 180 дней.
- **Зависимости.** wrangler 3 → 4.127, vitest 2 → 4.1, пакет
  `@cloudflare/vitest-pool-workers` переименован в `@cloudflare/vitest-plugin`.
  12 уязвимостей → 0. Заодно закрыто расхождение `compatibility_date`.
  Побочный эффект бампа: изоляция хранилища теперь на файл тестов, а не на
  тест, — чистка таблиц перенесена в `beforeEach`.

### Спринт 2 — безопасность и прод-готовность

- **Таймауты** на вызовы Telegram API (10 с) и на отправку формы заявки
  в FormSubmit (15 с). Без них зависший запрос держал воркер до предела
  и оставлял кнопку заблокированной навсегда.
- **Оповещение о падениях** в чат админов, с паузой в 5 минут.
- **Утечка токена в логи** — вычистка токена из текста ошибок.
- **Поток заявок** — потолок 3 заявки с чата за сутки, `/resend` пачками
  по 25 с явным остатком.
- **Персональные данные** — уборка затирает имя, вопрос и chat_id, а не
  только телефон.
- **Тихо сломанный деплой** — без `ADMIN_CHAT_ID`/`ADMIN_IDS` бот теперь
  отвечает 404, а не принимает заявки в никуда.
- Мелочи: `parseLeadId` на callback_data, `Object.hasOwn` на словарях.

### Спринт 3 — SEO и скорость

SEO оказался в основном уже сделан (LLMO-проход от 2026-08-31): полные
метатеги, JSON-LD с DrivingSchool, Course, FAQPage, BreadcrumbList.
Добавлено:

- `og:image:width/height/alt` на страницы билетов, тренировки, вопросов;
- **даты считаются из git**, а не пишутся литералом — `tools/update_sitemap.py`
  и `content_date()` в `build_llms_full.py`;
- **видео шапки не качается до решения о нём** — при `autoplay` браузер
  игнорировал `preload="metadata"` и тянул 1,6 МБ в окне LCP;
- `preload as=fetch` для `tickets-b-ru.json` — запрос уходил только после
  трёх последовательных раундтрипов за модулями.

### Спринт 4 — доступность тренажёров

- Контраст границ кнопок ответа и фильтров: было 2,16:1, WCAG требует 3:1.
- Правильный ответ передавался одним цветом — добавлены подписи для
  скринридера (`.sr-only`), разметка вынесена в общий `js/answer-marking.js`
  (была скопирована в два файла слово в слово).
- Экран результатов терял фокус; предупреждение об истекающем времени было
  только красным цветом.
- `aria-pressed` на фильтрах, `aria-invalid`/`aria-describedby` на форме,
  `preventDefault` на стрелках, `prefers-reduced-motion` для `exam.css`.
- Скачки вёрстки: у картинок билетов не было размеров, каждый вопрос сдвигал
  варианты ответа вниз.

### Спринт 6 — систематизация

- `CLAUDE.md` — стек, команды, границы модулей, неочевидные правила.
- README корня и бота обновлены под новое поведение.

## Осталось — требует решения владельца

| Пункт | Почему не сделано |
|---|---|
| **Описания к картинкам билетов** | 524 картинки, и для вопросов про дорожную ситуацию картинка *и есть* вопрос. Сейчас у всех один alt «Изображение к вопросу билета». Незрячий ученик такие билеты решать не может. Дёшево не чинится — нужен либо ручной труд, либо генерация с проверкой |
| **Анимация заголовка держит LCP** | `.marquee__inner` выезжает из-под обрезки 700 мс, и до конца анимации LCP не засчитывается. Это ядро визуального решения страницы — менять без вас не стал |
| **Экзамен качает 682 КБ ради 30 билетов** | Кнопка старта ждёт весь `tickets-b-ru.json` (92 КБ в gzip). Чинится разбиением данных — это переделка формата, а не правка |
| **Группа админов = доступ к персональным данным** | `ADMIN_IDS` определяет, кто может *нажимать*, но карточки видит любой участник группы, включая всю переписку выше. Решается отключением ссылки-приглашения, а не кодом |
| **Команда «забудь меня»** | Ученик не может удалить свои данные по запросу. Шесть строк кода, но это продуктовое решение |
| **Шесть начертаний шрифтов** | Грузятся три веса Unbounded и три Golos, странице видимо нужно меньше. Убирать лишние — визуальное решение |
| **Архив планов** | `.plan-*.md` в корне: пять файлов, часть выполнена. Переносить в `docs/plans/` не стал — вы на них ссылаетесь |

## Правило работы

Каждый пункт: тест → правка → все три набора тестов → коммит.


## Партнёрские билеты, 2026-09-24

Цель: Omio и Holafly в отдельном разделе под видео-шапкой.
Решение: сохранить форму билета, добавить Omio с купоном NEW10 и ссылкой https://omio.sjv.io/c/7837896/4029327/7385. Impact подтверждает скидку 10% новым клиентам, заказ до 80 €, срок до 30.11.2026 23:59 CEST. На билете показаны скидка и лимит; дата убрана по просьбе владельца. Создано напоминание на 30 ноября убрать купон. Мобильный вид проверен.
Этап: проверены desktop 1440px и mobile 320px, переполнения и ошибок консоли нет. Тесты 64/141/131 и typecheck прошли.
Публикация разрешена владельцем. Ссылки и diff проверены; локальные тесты 64/141/131 и typecheck прошли. Следующий шаг: проверить GitHub Pages и живой сайт после push.


### Горизонтальные купоны, 2026-09-24

По просьбе владельца купоны локально размещены в один ряд. На телефоне горизонтальная прокрутка с фиксацией карточки, доступная с клавиатуры; без автодвижения. CSS v6. Проверены desktop, 320 CSS px, прокрутка до Omio, отсутствие переполнения страницы и ошибок консоли. Следующий шаг: просмотр и разрешение на публикацию.

Уточнение владельца: сами купоны и компактная подпись перенесены поверх видео в шапку. Отдельного раздела под видео нет. CSS v8. Проверены desktop и 320 CSS px, отсутствие горизонтального переполнения. Версия локальная, публикация ожидает разрешения.

Последнее уточнение: горизонтальная прокрутка и полоска убраны. CSS v9, два компактных купона в сетке, на телефоне подряд вертикально. Оба видны без свайпа.

По запросу владельца убраны повторяющиеся подписи о комиссии с обоих купонов. Общий заголовок «Партнёрские предложения» и rel="sponsored noopener" сохранены. Проверка текстового diff пройдена.

Купонам задана непрозрачность 95% (opacity: 0.95), CSS v10. Правка локальная.

По последнему скриншоту купоны размещены слева один под другим, непрозрачность 85%, CSS v11. Локальное превью, без публикации.

## Аудит кода и интерфейса, 2026-09-26

Цель: предложить улучшения визуала, удобства и скорости с сохранением основных функций.
Этап: аудит завершён, изменения приложения не выполнялись.
Источник: запрос владельца, локальный main 0d798d6 и просмотр https://avtoshkola.ge/.
Это предложения для следующего этапа, а не утверждённый редизайн.

### Что сохранить

- Статические HTML/CSS/JS и отдельные модули чистой логики экзамена и тренировки.
- Текущие адреса страниц и вопросов, 921 вопрос, правила экзамена, ID и сохранённый прогресс.
- Узнаваемые цвета, шрифтовой характер, видео Тбилиси, партнёрские предложения и способы связи.
- Сохранение занятия, темы, ошибки, избранное и доступ без регистрации.
- Генераторы данных и тематических страниц, существующие проверки.

### Предлагаемый порядок

1. **Облегчить чат и убрать технические риски.**
   - Аватар 44x44 использует PNG 1254x1254 размером 1 235 240 байт.
     Назначение src выполняется сразу: js/chat.js:64.
     В браузере подтверждена загрузка картинки даже при закрытой панели.
     Подготовить маленький вариант с достаточной плотностью пикселей;
     загрузку можно отложить до открытия панели.
   - Импорты answer-marking.js в js/exam.js:19 и js/training.js:18 не имеют версии.
     Привести к принятому правилу cache-busting, проверять единообразие автоматически.
   - Добавить ограничение ожидания загрузки каталога цен в js/chat.js:171-181,
     чтобы при зависшем запросе показывалось предусмотренное предупреждение.
   - Видео уже учитывает reduced motion и экономию трафика. Не выдавать это за новую задачу.

2. **Восстановить приоритет обучения в первом экране.**
   - Купоны стоят перед H1: index.html:339-396. На экране 390x844 блок действий
     начинается примерно на y=703, на 1280x900 около y=754.
     Предложение: поднять заголовок и запись, купоны сделать компактнее или расположить
     ниже основных действий. Сохранить обе партнёрские ссылки и купоны.
     Их нынешнее размещение было явным решением владельца; новый вариант требует выбора дизайна.
   - Чат автоматически открывается через 10 секунд: js/chat.js:152-157.
     В первом просмотре он перекрыл значительную часть первого экрана.
     Предложение: открывать по нажатию или показывать компактную подсказку.
   - Различить пункт «Вопросы» о школе и «Все вопросы» ПДД: например,
     назвать первый «Частые вопросы».

3. **Сделать рабочий экран тренировки компактнее.**
   - На странице одновременно присутствуют два блока «Миссия на сегодня»,
     настройки, фильтры, общая статистика и сетка вопросов.
     После старта оставить вопрос, ответы и короткий прогресс на первом плане,
     настройки раскрывать по запросу. Сами режимы сохранить.
   - Кнопка старта уже прокручивает страницу к вопросу, а переход «Дальше» переносит
     фокус на новый вопрос. Эти работающие механизмы не нужно реализовывать заново.
   - Сохранённое занятие уже восстанавливается. Сделать продолжение более заметным,
     не менять формат хранения без миграции и регрессионной проверки.
   - Краткие объяснения после ошибки полезны как отдельный содержательный этап:
     только с проверенным источником, без автоматически выдуманных правил ПДД.

4. **Упростить сопровождение кода постепенно.**
   - Сборка кнопок ответов повторяется: js/exam.js:112-132 и js/training.js:446-466.
     Можно расширить существующий общий DOM-модуль, сохранив независимость *-logic.js.
   - Общие шапки и версии ресурсов проверять на согласованность.
     Вводить новые шаблоны только при оправданном объёме повторений.
   - Каталог создаёт 921 карточку за раз: js/ticket-catalog.js:61-80.
     Для проверок статуса использовать Set вместо повторных Array.includes
     в js/ticket-catalog-logic.js:11-14. Порционную отрисовку рассматривать
     после замера на слабом устройстве, поскольку выигрыш пока не измерен.

5. **Защитить изменения проверками пользовательских сценариев.**
   - 76 текущих Node-тестов проходят, но не проверяют DOM-сценарии.
   - Добавить небольшой браузерный набор: открыть каталог, начать тренировку,
     ответить, перейти дальше, восстановить занятие, начать экзамен, открыть/закрыть чат,
     проверить ошибки формы без отправки настоящей заявки.
   - Проверять мобильный экран и клавиатуру. Изменения данных, прогресса,
     расчёта результатов и внешней отправки выделять из визуальных правок.

### Реально проверено

- npm test: 76 passed, 0 failed, 0 skipped. Выполнено помощником gpt-5.6-sol.
- Живой сайт: главная, каталог, тренировка, старт экзамена, переход к ценам.
- Внешний вид на широком экране 1280x900 и мобильном 390x844.
- Ответ в тренировке, переход ко второму вопросу и восстановление после перезагрузки.
- Автооткрытие и закрытие чата, фактический размер загруженного аватара.
- На просмотренном пути предупреждений и ошибок в журнале браузера не получено.
- В .gitignore уже есть экранированный паттерн для личных каталогов заметок.

Ограничения: Lighthouse, полевые Core Web Vitals, медленная сеть, полный экзамен,
реальная отправка заявки, внешний API цен и официальный банк вопросов не проверялись.
Наборы бота и Python повторно не запускались: аудит не менял их код.
Рост конверсии и удержания не измерен, предложения по компоновке являются гипотезами.
Справка: [отложенная загрузка изображений](https://web.dev/articles/browser-level-image-lazy-loading).
Изображения первого экрана нельзя массово переводить на lazy-loading.

Следующий шаг: небольшой пакет оптимизации аватара, версий импортов и таймаута,
затем отдельный вариант первого экрана и компактного режима тренировки.
Технического блокера для подготовки этих изменений не обнаружено.
Commit, push и deploy в этой задаче не выполнялись.

## Первый пакет после аудита, 2026-09-26

Владелец разрешил начать ответом «го». Этап: первый пакет готов локально по specs/007-frontend-polish.
Цель: уменьшенный аватар с загрузкой при открытии, таймаут каталога цен и согласованные версии ресурсов.
Результат: аватар 2168 байт вместо 1235240, загрузка при открытии; таймаут каталога 10 секунд; согласованные версии модулей и CSS. Исправлен левый край чата на мобильном экране с полосой прокрутки.
Проверки: сайт 82/82, бот 141/141 и typecheck, Python 138/138; браузер 320/390/1280, таймаут, открытие/закрытие чата, ответы в обоих тренажёрах и продолжение сохранённого занятия.
Блокеров нет. Следующий шаг: отдельный визуальный пакет главной страницы и тренировки. Commit, push и deploy не выполнялись.

## Оставшиеся этапы выполнены локально, 2026-09-26

Источник: прямой запрос владельца «делай все этапы здесь и открой сайт». Спецификация: specs/008-ui-polish.

Результат:
- Главная: заголовок и запись раньше партнёрских предложений; на широком экране купоны справа, на телефоне ниже действий. Оба купона, условия, ссылки и видео сохранены.
- Чат открывается по нажатию, прежнего автоматического раскрытия через 10 секунд нет. Оптимизации пакета 007 сохранены.
- В общей навигации «Частые вопросы» отличены от каталога вопросов ПДД. На 320 px устранено обрезание последней ссылки.
- Тренировка: заметный блок продолжения, раскрываемые настройки, короткий прогресс и раскрываемая сетка вопросов. Поиск, темы, режимы, избранное, фокус и сохранение работают в прежнем формате.
- Кнопки ответов собираются общим DOM-модулем; чистая логика экзамена и обучения не менялась. Каталог один раз строит Set-индексы прогресса, наборы и номера, сохраняя все 921 вопрос.
- Добавлен Playwright runner и отдельная задача CI: 6 сценариев на трёх размерах экрана. Внешние запросы подменяются, реальные заявки не отправляются. Проверка версий дополнена проверкой общей шапки.

Реально проверено: 84 Node-теста, 141 тест бота и typecheck, 138 Python-тестов, 18 браузерных сценариев. Генераторы llms-full и sitemap выполнены, изменений не потребовали. Независимое ревью завершено, замечания исправлены. CUA: главная 1280/320, тренировка 1280/390, ответ, переход, продолжение, увеличение картинки и Escape; ошибок журнала на просмотренном пути тренировки нет.

Ограничения: реальная доставка заявок, внешний API, официальный банк после авторизации и полевые метрики не проверялись. Объяснения ПДД после ошибок остаются отдельной задачей с проверенными источниками. Порционная отрисовка каталога не добавлена: необходимости по измерениям пока не установлено.

Этапы разработки и проверки завершены. Следующий шаг: просмотр локального результата владельцем на http://127.0.0.1:8879/. Технических блокеров нет. Все изменения локальны; commit, push и deploy не выполнялись.
