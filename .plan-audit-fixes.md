# План: доводка репозитория по итогам аудита

Источник — отчёт аудита от 2026-09-01. Порядок: критично → желательно → опционально.
Один пункт = правка → тесты → коммит.

Базовые линии до начала: бот 109 тестов + typecheck чисто, Python 92, сайт 30.

## Уже закрыто соседней сессией (проверено, ничего не делаю)

- **К1.** `typecheck` падал на `crypto.subtle.timingSafeEqual` → коммит `fa7b44d`,
  `npm run typecheck` выходит с кодом 0.
- **Ж5.** `bot/README.md` со старым именем `WEBHOOK_SECRET` → коммит `8463f21`,
  в руководстве оба новых имени.

## Делаю

| # | Пункт | Почему берусь сам |
|---|---|---|
| 1 | **К2.** `/set` не ограничивает длину значения факта | Правильное поведение очевидно: рядом уже есть `NAME_LIMIT`/`QUESTION_LIMIT` для ввода ученика, факт — та же строка в том же сообщении Telegram |
| 2 | **Ж3.** `splitMessage` может разрубить HTML-сущность пополам | Ошибка с понятным правильным поведением; сначала тест, который её ловит |
| 3 | **Ж4a.** `renderLeadCard` печатает `undefined` при неизвестном статусе | Рядом уже есть `statusLabel()`, написанный ровно для этого |
| 4 | **Ж6.** Нет проверки, что `dlya-bota/*.txt` собраны из актуальных `.md` | Добавление теста на существующую логику, поведение не меняется |
| 5 | **С2.** `analytics.js`: `event.target.closest` без проверки на Element | Добавление проверки на границе, поведение при нормальном клике не меняется |
| 6 | **Т3.** `parse_tickets.py` качает картинку по любому хосту из разметки | Валидация входа на границе доверия |
| 7 | **Т4.** `strip_frontmatter` принимает markdown-разделитель `---` за YAML-шапку | Ошибка с очевидным правильным поведением, тест первым |
| 8 | **Т2.** Лишний `sleep` после последней неудачной попытки в ретраях | Однозначный баг, 6 секунд впустую перед исключением |

## НЕ делаю — оставляю на решение

| Пункт | Почему не сам |
|---|---|
| **Ж4b.** `CHECK` на `leads.status` в миграции | SQLite не умеет `ALTER TABLE ADD CONSTRAINT` — нужна пересборка таблицы с переносом данных. Изменение схемы на живой базе, риск потери заявок |
| **Ж7.** Rate limiting / капча на форме звонка | Настраивается на стороне FormSubmit и требует ключей — конфиг окружения |
| **О8a.** Чистка `lead_events` в `runCleanup` | Удаление данных. Срок хранения журнала действий — продуктовое решение, из кода не выводится |
| **О8c.** Захардкоженная дата в `build_llms_full.py` | Из кода не видно, намеренно ли она правится руками при публикации. В `llms.txt` рядом стоит такая же дата — похоже на осознанную ручную синхронизацию |
| **О8e.** Дублирование отрисовки ответов в `exam.js` / `training.js` | Архитектурная перестройка |
| **S3.** `applyFilter` сбрасывает сохранённую позицию | Изменение поведения, из кода не следует, что текущее неверно |
| **B8.** 12 уязвимостей в зависимостях | Все в wrangler/vitest, чинятся только мажорными бампами |
| **B9.** `compatibility_date` новее рантайма wrangler | Лечится мажорным обновлением wrangler |
| **О8b.** Шкала прогресса экзамена не доходит до 100 % | При проверке оказалось не багом: `index / QUESTION_COUNT` — это «сколько вопросов позади», и на последнем вопросе позади ровно 29 из 30. Менять нечего |
